#include<stdio.h>
#include<stdlib.h>
#include<time.h>

int main()
{
	srand(time(NULL));
	int i, dice1, dice2, dice_sum;
	int count[13] = { 0 };

	////////////////////////////////////////////
	for (i = 0; i < 36000; i++) {
		dice1 = rand() % 6 + 1;
		dice2 = rand() % 6 + 1;
		dice_sum = dice1 + dice2;
		count[dice_sum]++;
		//count[2] ~ count[12] 까지 저장되어있음
	}

	for (i = 2; i < 13; i++) {
		printf("%d : %d (%f)\n", i, count[i], (float)count[i] / 36000);
	}
	////////////////////////////////////////////


	return 0;


}

//(b) srand를 하지 않으면 rand()값이 한번 정해진값으로 계속 나와주기 때문이다.
// srand를 하면 rand()값을 할때마다 저장되는 곳이 달라지기떄문에 rand를 불러올때마다 다른값을 가져올수있다.
